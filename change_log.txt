Change log for proposal-only cleanup

Follow-up proposal update on 2026-06-15:

Proposed changes:
1. Remove non-functional Agent API metadata settings:
   - app_title
   - app_version
   - app_description

2. Make the Agent Knowledge Base URL explicit:
   - Remove the default value "http://127.0.0.1:8010".
   - Keep knowledge_base_url as a required runtime setting.
   - Keep main.py as the normal source of KNOWLEDGE_BASE_URL injection.

3. Remove policy_knowledge_index from Agent settings:
   - The policy route already determines that policy retrieval is being used.
   - A runtime setting should not decide the RAG path.

4. Simplify the Agent-to-Knowledge-Base search contract:
   - Remove index from KnowledgeSearchRequest.
   - Remove index from KnowledgeSearchResponse.
   - The current Knowledge Base search endpoint becomes policy-specific.

5. Simplify policy retrieval:
   - RetrievalPipeline.retrieve_policy no longer sends an index value.
   - It sends only query, top_k, law_name, article_id, as_of_date, and region.

6. Remove the generic local-domain Knowledge Base path:
   - Delete RetrievalPipeline.retrieve_domain.
   - Delete EvidenceAdapter.from_domain_chunk.
   - Remove DataDomainWorkflow local Knowledge Base scheduling and execution.
   - Remove DomainProfile.knowledge_index and its None values in domain profiles.

7. Simplify Knowledge Base Milvus alias configuration:
   - Replace index_aliases = {"policy": "tender_qa_policy"} with policy_collection_alias = "tender_qa_policy".
   - Use policy_collection_alias in KnowledgeBaseService.search and rebuild.py.

8. Remove Knowledge Base index branching:
   - Delete request.index == "policy" checks.
   - Run article-exact retrieval and policy scalar filtering unconditionally inside the policy Knowledge Base search path.
   - Remove unknown-index HTTP handling from knowledge_base_layer/api.py.

Follow-up proposal update on 2026-06-16:

Proposed changes:
1. Replace the old single-category classification contract with decomposition-first question understanding:
   - Add Category.UNCLEAR.
   - Add QuestionTask, QuestionDecomposition, and RoutePlan.
   - Delete QuestionClassification and RouteDecision.

2. Remove confidence-based routing:
   - Rewrite WorkflowRouter.decide to consume QuestionDecomposition.
   - Route by explicit child-task categories and the unclear category.
   - Remove routing_confidence_high and routing_confidence_low from Settings.

3. Update the classification chain and prompt:
   - Replace CLASSIFICATION_PROMPT with QUESTION_DECOMPOSITION_PROMPT.
   - Make QuestionClassifier return QuestionDecomposition.
   - Require child tasks to use one category each and express dependencies through depends_on.

4. Update application orchestration:
   - Store decomposition and route-plan state instead of classification and route-decision state.
   - Execute decomposed child tasks through a route plan.
   - Share entities, dependent child answers, evidence, citations, and tool events across child tasks.

5. Add final multi-task synthesis:
   - Add CompositeAnswerWorkflow.
   - Combine child workflow answers and shared evidence into one final answer.
   - Include composite_synthesis in generation metrics.

6. Update Agent stream and result metadata:
   - Format route events as question decomposition output.
   - Remove confidence display from route reasoning.
   - Return QuestionDecomposition in AskResult classification metadata.

7. Wire the new workflow dependencies:
   - Import and construct CompositeAnswerWorkflow in bootstrap.
   - Inject composite_workflow into ApplicationDependencies.

Follow-up proposal update on 2026-06-17:

Proposed changes:
1. Replace fixed final top-N retrieval with Self-RAG retrieval budgets:
   - Replace top_k with retrieval_batch_size.
   - Add max_retrieval_rounds and max_follow_up_queries.
   - Replace summarize_max_chunks and summarize_chunk_length with evidence_chunk_chars and evidence_context_chars.

2. Extend evidence sufficiency assessment:
   - Add flags for more local retrieval and official web search.
   - Let the model propose follow-up retrieval queries.
   - Let the model identify usable evidence IDs for final answer synthesis.

3. Add workflow-level Self-RAG helpers:
   - Add agent_layer/workflows/self_rag.py.
   - Centralize evidence merging, follow-up query selection, and final evidence selection.

4. Convert the policy workflow to an iterative Self-RAG loop:
   - Parse policy query conditions once.
   - Retrieve candidate evidence in batches.
   - Ask the model whether evidence is sufficient.
   - Continue local retrieval when the model requests it and the retrieval budget allows it.
   - Use official web search only when the model marks it necessary.

5. Update evidence prompt packing:
   - Include evidence_id in formatted evidence.
   - Enforce per-evidence and total evidence context budgets.
   - Use selected evidence for citations and final answer synthesis.

6. Update related workflows to use the new evidence budget names:
   - Composite answer synthesis uses evidence_chunk_chars and evidence_context_chars.
   - Data-domain answer synthesis uses the same evidence budget settings.
   - Website adapter calls use retrieval_batch_size as a candidate batch limit.

7. Apply stream_chunk_size consistently at the Agent transport boundary:
   - Clarify stream_chunk_size as the max Agent transport chunk size.
   - Chunk assistant, reasoning, and error output consistently in agent_layer/api.py.

V0.55 release

8. Remove incomplete policy website domain-suffix verification:
   - Drop policy_preferred_domains, urlparse, and PolicyWorkflow._is_preferred_domain.
   - Keep only SourceTier.OFFICIAL filtering until a real policy internet adapter verifies official sources.

9. Simplify SQL validator configuration:
   - Remove sql_max_joins, sql_max_subqueries, and sql_dialect from settings.
   - Remove the matching validator constructor state and join/subquery count checks.

10. Make SQL dialect and row limiting clearer:
   - Use an internal SQLite SQLGlot dialect constant for current parsing and normalization.
   - Keep sql_max_rows for read-only execution and LIMIT enforcement, with a clarifying comment.

11. Replace handmade workflow checkpointing with LangGraph checkpoint runtime:
   - Replace the custom SQLiteCheckpointStore with a LangGraph AsyncSqliteSaver runtime.
   - Keep the SQLite checkpointer open for the Agent app lifetime and close it on shutdown.

12. Convert policy Self-RAG into a LangGraph state machine:
   - Add a PolicyGraphWorkflow built with StateGraph.
   - Model parse, retrieve, assess, official-internet fallback, and synthesize as graph nodes.

13. Add conditional Self-RAG graph routing:
   - Route from assessment back to retrieval, to official internet search, or to synthesis.
   - Reassess evidence after successful official-internet supplementation.

14. Preserve workflow progress and output contracts:
   - Keep ToolEvent progress callbacks and WorkflowResult output.
   - Return the LangGraph run_id as the checkpoint thread_id for persisted graph state.

15. Remove fake data-domain checkpoint writes:
   - Stop injecting the handmade checkpoint store into data-domain workflows.
   - Remove manual planned/running/delete checkpoint calls until data-domain workflows are separately migrated to LangGraph.

Follow-up proposal update on 2026-06-18:

Proposed changes:
1. Move Agent user-facing response copy out of Settings:
   - Add agent_layer/conversation/quick_responses.py for small-talk keywords and canned replies.
   - Add agent_layer/conversation/fallback_messages.py for no-evidence fallback messages.

2. Keep Agent Settings focused on runtime configuration:
   - Remove no_results_response, source_unavailable_response, greeting_responses, greeting_keywords, thanks_keywords, and goodbye_keywords from agent_layer/config.py.

3. Simplify LLM API key handling:
   - Remove Settings.effective_llm_api_key.
   - Use settings.llm_api_key directly in ModelFactory.
   - Keep LLM_API_KEY and LLM_API_URL in the root .env.

4. Preserve dynamic service URL injection:
   - Do not put KNOWLEDGE_BASE_URL in .env.
   - Keep main.py as the source of the dynamically assigned Knowledge Base URL through the Agent process environment.

5. Fix the data-domain no-evidence fallback path:
   - Remove the stale self.checkpoint_store reference.
   - Return SOURCE_UNAVAILABLE_RESPONSE from the dedicated fallback message module.

6. Simplify the Agent stream request and context path:
   - Remove session_context and generation_options from AgentStreamRequest.
   - Stop passing SessionContext through context resolution, Agent orchestration, workflows, website adapters, SQL gateway execution, and SQL evaluation.
   - Resolve conversation context from user_message and persisted history only.

7. Always stream Agent progress as reasoning:
   - Remove the include_progress switch.
   - Emit all Agent progress events unconditionally so the transport layer continues sending them as reasoning chunks.

8. Tighten Agent message and error transport contracts:
   - Restrict Message.role to user and assistant because the Backend never supplies system messages.
   - Remove code and error_id from AgentTransportChunk and StreamEvent because no current component consumes them.
   - Simplify Agent error-event and transport-chunk construction accordingly.

9. Remove the unused non-streaming and final-event path:
   - Delete TenderQAApplication.ask, AskResult, StreamEventType.FINAL, and all final-event emissions.
   - Delete _route_name, _final_event, _source_distribution, final-event metrics, and unconsumed final metadata.
   - Keep StreamEvent.data because active route, progress, and source formatting still consumes it.

10. Fix structured output to the project implementation choice:
   - Remove structured_output_method from Agent Settings.
   - Pass method="json_mode" directly at every with_structured_output call site.

V0.57 release

Follow-up proposal update on 2026-06-20:

Proposed changes:
1. Unify conversational contextualization and question decomposition:
   - Extend QuestionDecomposition with standalone_question, resolved_entities, ambiguous, and clarification_question.
   - Give the structured classifier the current question and bounded recent conversation.
   - Handle anaphora, elliptical follow-ups, ambiguity, entity resolution, classification, and task decomposition in one model call.

2. Remove the separate context-resolution path:
   - Delete ContextResolver, QuestionRewriter, their prompts, regex fast path, bootstrap wiring, and dedicated error type.
   - Remove obsolete context-resolution, trace, conversation, run-state, metric, and error schemas.

3. Simplify Agent orchestration around unified question understanding:
   - Format recent history once for classification and general-answer prompts.
   - Pass the original question, standalone question, resolved entities, and formatted history explicitly to downstream workflows.
   - Clarify ambiguous references before routing instead of guessing an entity.

4. Align classification evaluation with decomposition output:
   - Replace single-category samples and confusion-matrix logic with expected category sets.
   - Evaluate exact category-set matches and multi-label macro F1.

Follow-up proposal update on 2026-06-22:

Proposed changes:
1. Harden the child-task schema contract:
   - Add schema-level validation for clarification_question.
   - Require unclear child tasks to provide clarification_question.
   - Reject clarification_question on executable child tasks.

2. Make classification and routing outputs non-empty by contract:
   - Require ChildTaskList to contain at least one child task.
   - Make RoutePlan.tasks required instead of defaulting to an empty list.
   - Remove router fallback behavior for impossible empty classifications.

3. Remove generic entity-recognition models from active contracts:
   - Delete EntityHint.
   - Remove entities from ResearchPlan.
   - Remove entities from WebsiteQuery.

4. Update prompts to reflect the current bounded-history design:
   - Replace complete-conversation wording with recent-conversation wording.
   - Keep anaphora resolution inside child-task question generation.
   - State that generic entity extraction is deferred until concrete SQL and website tool needs are known.

5. Simplify deterministic routing:
   - Remove the router Settings dependency and constructor.
   - Route to clarification when any child task carries clarification_question.
   - Route all-OTHER child-task lists to general answer and the rest to workflow execution.

6. Simplify clarification aggregation:
   - Return every task-level clarification question.
   - Remove the arbitrary three-question limit.
   - Remove the fallback clarification text that contradicts the validated schema.

7. Align bootstrap and application cleanup with the router revision:
   - Construct WorkflowRouter without runtime settings.
   - Delete the unused TenderQAApplication._category_label helper.

8. Restore the full policy LangGraph state contract:
   - Add evidence, assessment, retrieval query bookkeeping, round index, model-call count, tool events, result, and internet evidence count to PolicyGraphState.
   - Prevent the truncated state type from dropping graph data required by retrieval, assessment, routing, checkpointing, and synthesis.

9. Remove generic entities from data-domain website execution:
   - Put needed subjects, keywords, time, region, fields, and metrics into existing ResearchPlan fields and task goals.
   - Stop passing a removed entity list to website adapters.

10. Correct progress and answer-generation prompt wording:
   - Describe question understanding as using recent conversation rather than complete conversation.
   - Label bounded history consistently in data-domain, policy, general, and composite prompts.
   - Emphasize that history is context for understanding, not evidence or a reliable factual source.

V0.58 release

Follow-up proposal update on 2026-06-23:

Proposed changes:
1. Replace exact quick-response phrase matching with a model-classified intent contract:
   - Add QuickResponseType and QuickResponseDecision to the Agent schema layer.
   - Cover greeting, thanks, goodbye, capabilities, wellbeing, acknowledgement, compliment, apology, and none.
   - Treat none as the route into the existing normal workflow.

2. Add a dedicated quick-response classifier and canned response script:
   - Create a pre-classifier that decides whether the latest message is pure social or conversational intent.
   - Require business questions, follow-ups, corrections, and mixed social-plus-business messages to return none.
   - Key all canned replies by classified quick-response intent instead of normalized raw text.

3. Wire quick-response pre-classification into Agent orchestration:
   - Keep empty input as a deterministic canned response.
   - Run quick-response classification before normal question decomposition for non-empty input.
   - Inject the quick-response classifier through bootstrap and ApplicationDependencies.

V0.59 release

Follow-up proposal update on 2026-06-26:

Proposed changes:
1. Simplify the quick-response structured output:
   - Remove the unused reasoning field from QuickResponseDecision.
   - Keep quick-response classification focused on the intent value consumed by runtime code.

2. Tighten TenderQAApplication.stream input handling:
   - Require history_messages as a list to match the Agent API contract.
   - Remove the redundant None fallback.
   - Strip user_message once and reuse the normalized message consistently.

3. Clean up Agent application maintenance details:
   - Add the missing dataclass import.
   - Remove the unused Evidence import.
   - Document _history_text history bounding and serialization behavior.

Follow-up proposal update on 2026-06-27:

Proposed changes:
1. Add a local Hugging Face LLM provider:
   - Keep OpenAI-compatible API mode as the default.
   - Add local_huggingface mode with optional Transformers, Torch, and Accelerate dependencies.
   - Add settings for device_map, torch_dtype, and trust_remote_code.

2. Preserve the existing structured-output contract:
   - Keep workflow call sites using with_structured_output(..., method="json_mode").
   - Implement json_mode support in the local Hugging Face chat wrapper.
   - Parse generated JSON text and validate it with the requested Pydantic schema.

3. Complete local model invocation:
   - Load the Hugging Face tokenizer and causal language model once through a shared runtime.
   - Use the tokenizer chat template for message formatting.
   - Support synchronous and asynchronous LangChain chat generation with timeout handling.

4. Update model factory and structured SQL generation:
   - Return either ChatOpenAI or the local Hugging Face chat model through the BaseChatModel contract.
   - Share one local runtime between structured and answer models.
   - Apply structured-output retries to SQL candidate generation as well.

5. Add a project-wide prompt revision guide:
   - Start prompts from the runtime task.
   - Keep only information useful for completing the task.
   - Match prompt wording to actual Pydantic fields and runtime behavior.

6. Remove raw Pydantic JSON Schema from model-facing prompts:
   - Stop passing model_json_schema output through json.dumps.
   - Replace schema dumps with compact JSON examples and field explanations.
   - Keep Pydantic validation after generation.

7. Keep structured-output instructions inside each task prompt:
   - Remove the local Hugging Face wrapper's extra generic system prompt.
   - Let each structured prompt define its own exact JSON output contract.

8. Revise all structured classification, parsing, planning, and SQL prompts:
   - QUICK_RESPONSE_PROMPT.
   - QUESTION_DECOMPOSITION_PROMPT.
   - POLICY_ASSESSMENT_PROMPT.
   - POLICY_QUERY_PROMPT.
   - RESEARCH_PLAN_PROMPT.
   - SQL_GENERATION_PROMPT.

9. Revise all answer-generation prompts:
   - POLICY_ANSWER_PROMPT.
   - DOMAIN_ANSWER_PROMPT.
   - GENERAL_PROMPT.
   - COMPOSITE_ANSWER_PROMPT.

10. Format model-facing configuration as readable text:
   - Format domain profiles, research plans, and SQL view schemas as labeled text.
   - Avoid feeding large JSON-serialized configuration objects to a local 14B model.

11. Fix quick-response schema/runtime alignment:
   - Use quick_response_type consistently.
   - Replace the placeholder JSON value with a concrete example.
   - Remove the unused schema variable from quick-response invocation.

12. Preserve grounding and safety boundaries:
   - Treat history only as user-intent context.
   - Treat evidence, child answers, web content, and database rows as data, not instructions.
   - Keep citation and evidence-only constraints in synthesis prompts.

13. Remove global child-task routing:
   - Delete WorkflowRouter and RoutePlan.
   - Stop deciding clarify, general_answer, or execute for the whole decomposed task list.
   - Route each child task at execution time according to its category.

14. Add explicit child-task execution statuses:
   - Add TaskStatus with solved, unsolved, and blocked states.
   - Extend WorkflowResult with status and unresolved_reason.
   - Add ChildTaskOutcome for final synthesis and dependency tracking.

15. Execute decomposed tasks by dependency graph:
   - Replace the route-plan executor with a child-task graph executor.
   - Run ready independent tasks concurrently.
   - Detect missing dependencies and cyclic dependency cases as blocked outcomes.

16. Support partial completion instead of all-or-nothing clarification:
   - Treat unclear child tasks as unsolved.
   - Continue executing independent solvable tasks.
   - Mark downstream tasks blocked when they depend on unsolved or blocked tasks.

17. Convert child-workflow failures into child-task outcomes:
   - Catch child-level AgentError during task execution.
   - Return unsolved WorkflowResult values instead of failing the entire request.
   - Preserve user-facing unresolved reasons for final synthesis.

18. Mark insufficient evidence as unsolved:
   - Report no-evidence policy tasks as unsolved.
   - Report insufficient policy evidence as unsolved while preserving partial evidence.
   - Report no-evidence data-domain tasks as unsolved.

19. Update final multi-task synthesis:
   - Pass child task results instead of only child answers.
   - Require final synthesis to explain solved, unsolved, and blocked tasks.
   - Keep shared evidence and citation validation in the composite answer path.

20. Use solved dependency results only as context:
   - Build dependent task prompts from solved prerequisite outcomes.
   - Exclude unsolved and blocked prerequisite tasks from factual child-task context.
   - Explain blocked dependents separately in the final answer.

21. Remove redundant structured-output type guards:
   - Remove isinstance checks after with_structured_output in classification and quick response chains.
   - Remove the same checks from policy assessment, policy query parsing, research planning, and SQL generation.
   - Keep schema validation inside structured-output invocation.

22. Clean up composition after router removal:
   - Remove WorkflowRouter construction from bootstrap.
   - Remove router from ApplicationDependencies.
   - Remove router imports from Agent application wiring.

V0.60 release

23. Add readable step comments to TenderQAApplication.stream:
   - Mark quick response, request decomposition, task-plan emission, child-task graph execution, progress relay, and final answer streaming.
   - Keep quick-response detection and direct reply as one logical step.

24. Improve top-level StreamEvent wording:
   - Make quick-response, decomposition, task-plan, graph-execution, final-answer, and generic-error messages reflect the real runtime action.
   - Use direct user-facing Chinese instead of internal process labels.

25. Centralize user-facing progress text:
   - Add a progress-content formatter based on ToolEvent stage and status.
   - Avoid exposing raw internal summaries or English workflow labels directly to users.

26. Make policy progress messages action-accurate:
   - Describe policy condition parsing, retrieval rounds, evidence sufficiency checks, official-web fallback, and policy answer synthesis.
   - Report policy retrieval and official-web evidence counts through structured event details.

27. Make data-domain progress messages action-accurate:
   - Describe research planning, SQL execution, website retrieval, data synthesis, skipped sources, and failed sources in user-facing wording.
   - Report website evidence counts and adapter failure counts through structured event details.

28. Improve route-event transport formatting:
   - Present child tasks as task id, question, and category.
   - State whether any child task needs newer or real-time data without using internal classification wording.

29. Improve progress-event transport formatting:
   - Clarify policy evidence sufficiency results.
   - Describe research-plan goals without claiming tasks will run sequentially when they may run concurrently.

30. Improve source-event transport formatting:
   - Say that a source has been added as answer evidence.
   - Avoid wording that implies the source is still being confirmed after evidence processing is already complete.

31. Remove semantic reasoning formatters from the API layer:
   - Delete _format_route, _format_progress, and _format_source.
   - Remove category and source label maps that only supported those formatters.

32. Make StreamEvent.content authoritative for reasoning output:
   - Combine route, reasoning-summary, progress, and source events into one reasoning branch.
   - Transport the event content directly with only a simple bullet wrapper.

33. Move source wording to the application layer:
   - Add TenderQAApplication._source_content.
   - Emit user-facing source text before the API transport boundary.

34. Add an explicit child-task graph model:
   - Add TaskGraph with ordered tasks, task lookup, and dependency edges.
   - Keep graph construction separate from graph execution.

35. Build the task graph as Step 3 in the main stream flow:
   - Parse classifier output into TaskGraph after decomposition.
   - Emit task-plan events from the graph rather than raw child-task output.

36. Execute the explicit task graph:
   - Pass TaskGraph into _execute_child_tasks.
   - Initialize pending execution state from the graph task map.

37. Use graph dependency edges during scheduling:
   - Check missing dependencies through TaskGraph.dependencies.
   - Decide task readiness from graph edges instead of rereading raw task fields.

38. Add explicit quick-response route wording:
   - Add route_content to each canned quick response.
   - Use route_content for route events instead of exposing the internal quick-response kind.

39. Move task-graph details into route content:
   - Include child task ids, questions, categories, dependencies, and freshness needs in user-facing route text.
   - Keep StreamEvent.content as the reasoning text consumed by the API layer.

40. Move execution-strategy details into reasoning content:
   - Include task categories and freshness summary in the reasoning-summary event.
   - Preserve the dependency-graph execution explanation in user-facing wording.

41. Move source metadata into source content:
   - Include source domain, source type, title, legal position, publish date, and source location in source reasoning text.
   - Remove redundant StreamEvent.data payloads from route, progress, and source emissions after their useful meaning is reflected in content.

V0.61 release

Follow-up proposal update on 2026-06-28:

Proposed changes:
1. Keep route disclosure and execution reasoning as separate stream events:
   - Keep ROUTE for the decomposed child-task graph and dependency/freshness information.
   - Keep REASONING_SUMMARY for the execution strategy over that graph.
   - Do not combine the two event types because they answer different user-facing questions.

2. Move child-task graph validation and construction into TaskGraph:
   - Make TaskGraph accept only the child task list.
   - Build task_ids, task_map, dependencies, and frontier_task_ids inside __init__.
   - Reject duplicate child task IDs and missing dependencies during graph construction.

3. Keep TaskGraph lightweight:
   - Remove the separate _tasks storage.
   - Remove the public tasks property.
   - Expose ordered task_ids plus copied task_map, dependencies, and frontier_task_ids accessors.

4. Remove the old graph-building helper:
   - Delete TenderQAApplication._build_task_graph.
   - Construct TaskGraph(child_tasks) directly in the main stream flow after decomposition.

5. Update task-graph consumers to use the new graph accessors:
   - Update _task_graph_content and _execution_strategy_content to derive task objects from task_ids and task_map.
   - Update _execute_child_tasks to initialize pending work and dependency checks from task_map and dependencies.
   - Build final child_results in graph task-id order.

6. Rename decomposition failures away from generic classification errors:
   - Replace ClassificationError with DecompositionError for child-task decomposition failures.
   - Raise DecompositionError for structured decomposition failures, duplicate task IDs, and invalid dependencies.
   - Treat DecompositionError as a fatal Agent-level error because the system cannot safely determine the next workflow.

7. Make quick-response classification recover locally:
   - Remove QuickResponseError from the active error flow.
   - Catch quick-response classifier exceptions inside QuickResponseClassifier.classify.
   - Return QuickResponseType.NONE on quick-response failure so the Agent continues into the normal workflow.

8. Clarify Agent error semantics:
   - Keep decomposition and final answer-generation failures as real Agent-level failures.
   - Treat optional quick-response failure as a non-fatal shortcut miss.
   - Treat child workflow and data-source failures as child-task outcomes that final synthesis can explain.

9. Convert child-task AgentError cases into unresolved workflow results:
   - Catch child-level AgentError during child-task execution.
   - Return WorkflowResult with status=UNSOLVED and unresolved_reason instead of aborting the whole request.
   - Mark dependent child tasks blocked when prerequisite outcomes are not solved.

10. Make data-domain no-evidence failures recoverable:
   - When SQL or website execution produces source errors but no evidence, return an UNSOLVED WorkflowResult.
   - Preserve the source error user_message as the unresolved_reason passed into final synthesis.
   - Keep the generic source-unavailable fallback only when there is no specific collected source error.

11. Add typed recoverable data-source errors:
   - Add SQLExecutionError for SQL executor, service, or permission failures.
   - Add WebsiteUnavailableError for website adapter or network-source failures.
   - Update user-facing messages to state the failed subsystem without implying the entire Agent failed.

12. Preserve SQL audit logging while typing SQL execution failures:
   - Keep execution_failed SQLAuditEvent recording in ReadOnlySQLGateway.
   - Raise SQLExecutionError after recording executor failures.
   - Let DataDomainWorkflow convert SQLExecutionError into a recoverable child-task error result.

13. Add LLM concurrency control for API and local model providers:
   - Add llm_concurrency_limit to Agent Settings.
   - Use one shared ModelConcurrencyGate inside ModelFactory for all structured and answer models.
   - Default local Hugging Face runtime to one concurrent call because GPU memory scales with concurrent generations.
   - Default OpenAI-compatible API mode to controlled parallelism while still allowing explicit override.

14. Wrap chat models with the shared concurrency gate:
   - Add ModelConcurrencyGate and ConcurrencyLimitedChatModel.
   - Gate async normal generation and async structured-output calls.
   - Prevent two concurrent Agent requests from invoking the same local model runtime at the same time by default.

V0.62 release

Follow-up proposal update on 2026-06-28:

Proposed changes:
1. Move child-task graph validation into TaskGraph:
   - Validate duplicate task IDs before building task_map.
   - Reject missing dependencies during graph construction.
   - Detect cyclic dependencies with a concise topological pass and expose dependents for runtime scheduling.

2. Replace child-task execution with dynamic DAG scheduling:
   - Start dependency-free tasks immediately.
   - Use asyncio.wait(..., FIRST_COMPLETED) so dependents can start as soon as their own prerequisites finish.
   - Pass progress_queue.put directly and receive it as enqueue_progress_event inside the workflow path.

3. Simplify child-task outcome data:
   - Remove category, question, and depends_on from ChildTaskOutcome.
   - Resolve task metadata from ChildTask via task_id when building dependent context and final synthesis input.
   - Remove _blocked_outcome and represent blocked tasks with answer=None plus a reason.

4. Clarify child-task result contracts:
   - Rename ChildTaskOutcome.reason to unresolved_reason.
   - Keep WorkflowResult separate from ChildTaskOutcome.
   - Keep WorkflowResult.run_id for workflow run identity and policy checkpoint lookup.
   - Import Literal in agent_layer/schemas.py because active schema fields still use it.

5. Split TenderQAApplication responsibilities:
   - Extract StreamEventFormatter for route, reasoning, progress, source, and answer chunk formatting.
   - Extract ChildWorkflowRunner for one-child-task workflow dispatch.
   - Extract ChildTaskExecutor for dependency scheduling and final composite synthesis.
   - Keep TenderQAApplication as the request-lifecycle facade.

6. Make the child-task boundary return task-aware outcomes:
   - Let ChildWorkflowRunner return ChildTaskOutcome directly.
   - Keep policy, data-domain, general, and composite workflows returning WorkflowResult where appropriate.
   - Remove the separate _outcome_from_result conversion helper.

7. Improve child-task execution naming and synthesis input assembly:
   - Distinguish ChildTask domain objects from asyncio.Task execution handles.
   - Use clearer names such as running_executions, start_child_task, and block_child_task.
   - Build final child_results from ChildTask metadata plus ChildTaskOutcome data.

8. Rename question-understanding API to decomposition:
   - Rename QuestionClassifier to QuestionDecomposer.
   - Rename classify() to decompose().
   - Rename the injected ApplicationDependencies field from classifier to decomposer.

9. Move decomposition modules out of the classification package:
   - Move agent_layer/classification/prompts.py to agent_layer/question_decomposition/prompts.py.
   - Move agent_layer/classification/chain.py to agent_layer/question_decomposition/decomposer.py.
   - Update application, bootstrap, and evaluation imports to the new module path.

10. Rename decomposition evaluation files and models:
   - Move agent_layer/evaluation/classification.py to agent_layer/evaluation/decomposition.py.
   - Rename ClassificationSample to DecompositionSample.
   - Rename ClassificationEvaluation to DecompositionEvaluation.
   - Rename evaluate_classification to evaluate_decomposition.

11. Preserve final synthesis behavior after the refactor:
   - Keep ChildTaskOutcome focused on outcome state only.
   - Keep task category, question, and dependency metadata sourced from ChildTask.
   - Continue requiring final synthesis to explain solved, unsolved, and blocked child tasks.

V0.63 release

12. Simplify task and workflow status contracts:
   - Remove the small TaskStatus enum.
   - Use Literal["solved", "unsolved"] for WorkflowResult.status.
   - Use Literal["solved", "unsolved", "blocked"] for ChildTaskOutcome.status.

13. Make child-task DAG scheduling fail fast:
   - Remove delayed unsolved-dependency tracking.
   - Block pending dependents immediately when a prerequisite is not solved.
   - Consume dependency edges only for solved prerequisites.

14. Harden child-task dependency validation:
   - Reject duplicated depends_on entries in ChildTask validation.
   - Keep duplicate task-id, missing dependency, and cycle validation in TaskGraph.

15. Tighten child workflow execution scope:
   - Remove original_question from child workflow dispatch.
   - Keep policy child answers scoped to the self-contained child task.
   - Store unclear child tasks with answer=None and unresolved_reason set.

16. Pass freshness requirements through all child workflows:
   - Pass task.requires_fresh_data to general, policy, and data-domain workflows.
   - Let policy freshness trigger a one-shot official internet check.
   - Let data-domain freshness force website retrieval even when the plan prefers SQL.

17. Update policy workflow status and prompt behavior:
   - Remove original_question from policy graph state and policy answer input.
   - Track requires_fresh_data and internet_searched in policy graph state.
   - Return workflow-level "unsolved" literals for no-evidence and insufficient-evidence outcomes.

18. Add optional internet support to the general workflow:
   - Make GeneralWorkflow return WorkflowResult.
   - Use optional general internet search for fresh general questions.
   - Ground fresh general answers in web evidence and citations, or return an unsolved source-unavailable result.

19. Wire general internet dependency injection:
   - Add general_internet_client to build_application.
   - Construct GeneralWorkflow with settings, EvidenceAdapter, and the optional general internet client.

20. Add an explicit dependency-outcome contract:
   - Add DependencyOutcome with task_id, question, and answer.
   - Use it as the narrow payload for solved prerequisite conclusions.
   - Keep dependency context separate from full child outcome state.

21. Move dependency extraction into the child-task scheduler:
   - Build direct dependency outcomes inside start_child_task.
   - Stop passing all child outcomes and task_map into ChildWorkflowRunner.
   - Keep the child workflow runner focused on category dispatch.

22. Stop merging dependency conclusions into child questions:
   - Delete ChildWorkflowRunner._task_question.
   - Pass task.question unchanged to each downstream workflow.
   - Pass dependency outcomes as a separate workflow argument.

23. Add shared dependency-outcome formatting:
   - Add format_dependency_outcomes in the common workflow helpers.
   - Use the same formatted dependency context across general, policy, and data-domain prompts.
   - Preserve task_id in the formatted context for traceability.

24. Extend the general workflow boundary:
   - Accept dependency outcomes as a separate input.
   - Add the same optional progress callback shape used by policy and data workflows.
   - Include dependency outcomes in GENERAL_PROMPT without altering the child question.

25. Stream general internet progress:
   - Emit started, skipped, failed, and completed general_internet ToolEvents.
   - Add StreamEventFormatter.progress_content handling for general_internet.
   - Preserve returned tool_events for final workflow accounting.

26. Extend policy parsing, assessment, and synthesis with dependency context:
   - Pass dependency outcomes through PolicyGraphState.
   - Include dependency outcomes in policy query parsing and evidence assessment.
   - Include dependency outcomes in policy answer synthesis as a separate prompt field.

27. Extend data-domain planning and synthesis with dependency context:
   - Pass dependency outcomes into ResearchPlanner.plan.
   - Include dependency outcomes in data-domain research planning.
   - Include dependency outcomes in data-domain answer synthesis.

28. Update downstream workflow signatures consistently:
   - Update GeneralWorkflow.run, PolicyGraphWorkflow.run, and DataDomainWorkflow.run.
   - Thread dependency outcomes through internal helper calls.
   - Keep secondary data-domain categories and freshness flags as explicit arguments.

29. Fix unresolved child-task answer semantics:
   - Return answer=None when ChildWorkflowRunner catches AgentError.
   - Store the user-facing failure message only in unresolved_reason.
   - Convert unsolved WorkflowResult answers to None in ChildTaskOutcome.

30. Fix the undefined TaskStatus reference:
   - Replace TaskStatus.SOLVED with the active "solved" literal.
   - Keep scheduler behavior aligned with the current Literal status schema.
   - Avoid a runtime NameError after a child task completes.

V0.64 release
